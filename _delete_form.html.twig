<form style="margin-top:-49.5px;margin-left:71px;" method="post" action="{{ path('app_transport_delete', {'id': transport.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transport.id) }}">
    <button style="width:30px;" class="btn btn-block btn-danger btn-sm"><i class="fa fa-trash"></i></button>
</form>
